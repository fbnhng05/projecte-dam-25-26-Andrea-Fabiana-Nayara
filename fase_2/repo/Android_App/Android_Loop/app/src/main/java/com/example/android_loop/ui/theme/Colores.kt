package com.tuapp.ui.theme

import androidx.compose.ui.graphics.Color

// Primary colors (color principal de la app)
val Primary = Color(0xFF003459)        // Azul oscuro
val OnPrimary = Color(0xFFFFFFFF)      // Color del texto sobre el primary

// Secondary colors
val Secondary = Color(0xFF007EA7)      // Azul océano
val OnSecondary = Color(0xFFFFFFFF)    // Texto sobre secondary

// Background colors
val Background = Color(0xFFF5F7FA)     // Fondo general de la app
val OnBackground = Color(0xFF1A1A1A)   // Texto sobre el fondo

// Surface (cards, dialogs, etc.)
val Surface = Color(0xFFFFFFFF)        // Fondo de tarjetas
val OnSurface = Color(0xFF1A1A1A)      // Texto en tarjetas

// Estados
val Error = Color(0xFFE74C3C)          // Rojo error
val OnError = Color(0xFFFFFFFF)        // Texto sobre error

// Colores extra de apoyo
val PrimaryContainer = Color(0xFF33658A)  // Variante más clara del primary
val Tertiary = Color(0xFF5BC0EB)          // Color de acento